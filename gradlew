#!/bin/sh
echo "Для сборки из терминала сначала выполните: gradle wrapper (Gradle 8.9),"
echo "после чего появится gradle/wrapper/gradle-wrapper.jar и этот скрипт заработает."
echo "Либо просто откройте проект в Android Studio."
